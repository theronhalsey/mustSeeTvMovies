<h1>Must See TV and Movies!</h1>

<h2>Dude, you need to see this!</h2>

<ul>
  {{#each moviesTv}}
    {{#unless seen}}
      {{> moviesTv/movieShow-block seen=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Seen dat!</h2>

<ul>
  {{#each moviesTv}}
    {{#if seen}}
      {{> moviesTv/movieShow-block seen=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Movie or TV show</h2>
<form class="create-form">

  <div class="form-group">
    <label for="movieShow">movieShow Name:</label>
    <input type="text" id="MovieShow" name="name">
  </div>

  <button type="submit">Add Movie/TV Show</button>
</form>
